# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Add global --json flag, scrub JSON output, explicit push mode flags."
# Optional context explaining why these changes were made
context = """
Push now requires an explicit mode flag (--only-head, --only-branches, --only-tags, --both-branches-and-tags) \
instead of implicit current-branch default and refspec positional args. \
Scrub match and scrub file support --json for machine-readable output (commit SHA mapping, tag rewrites). \
This unblocks rlsbl release scrub (upstream consumer). \
Scrub commands now respect --quiet. All post-rewrite suggestions route through safegit push."""
include = ["go", "docker"]
exclude = []
