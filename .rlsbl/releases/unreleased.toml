# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Sync submodule working trees after scrub history rewrites."
# Optional context explaining why these changes were made
context = "v0.17.1 added working-tree sync for the parent repo but missed submodule child repos. Submodule files on disk could still contain pre-scrub secrets."
include = ["go", "docker"]
exclude = []
